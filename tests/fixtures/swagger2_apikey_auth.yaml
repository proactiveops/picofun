swagger: "2.0"
info:
  title: Test API
  version: "1.0"
host: api.example.com
basePath: /v1
schemes:
  - https
securityDefinitions:
  apiKeyAuth:
    type: apiKey
    in: header
    name: X-API-Key
security:
  - apiKeyAuth: []
paths:
  /test:
    get:
      operationId: getTest
      responses:
        "200":
          description: OK
